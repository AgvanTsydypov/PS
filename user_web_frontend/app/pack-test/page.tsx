import { PackOpeningModal } from "../../components/pack/PackOpeningModal";

export const metadata = {
  title: "Pack Opening — Prototype",
};

export default function PackTestPage() {
  return <PackOpeningModal />;
}
